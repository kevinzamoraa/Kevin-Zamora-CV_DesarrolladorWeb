import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Kevin-Zamora-CV_DesarrolladorWeb',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
